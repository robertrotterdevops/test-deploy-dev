apiVersion: apps/v1
kind: Deployment
metadata:
  name: AKS-04
  labels:
    app: AKS-04
    managed-by: flux
spec:
  replicas: 1
  selector:
    matchLabels:
      app: AKS-04
  template:
    metadata:
      labels:
        app: AKS-04
    spec:
      containers:
      - name: AKS-04
        image: AKS-04:latest
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 5
