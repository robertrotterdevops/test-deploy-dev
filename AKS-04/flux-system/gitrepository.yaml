apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: AKS-04
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/your-org/AKS-04
  ref:
    branch: main
  ignore: |
    # Exclude files from synchronization
    /*.md
    /docs
    /scripts
    /overlays
