apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/name: Azure-03
  app.kubernetes.io/managed-by: argocd

resources:
  # Add your base resources here
  # - deployment.yaml
  # - service.yaml
  # - configmap.yaml

# ConfigMap generator
configMapGenerator:
  - name: Azure-03-config
    literals:
      - APP_NAME=Azure-03
