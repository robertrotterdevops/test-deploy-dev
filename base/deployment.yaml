apiVersion: apps/v1
kind: Deployment
metadata:
  name: Azure-02
  labels:
    app: Azure-02
    managed-by: flux
spec:
  replicas: 1
  selector:
    matchLabels:
      app: Azure-02
  template:
    metadata:
      labels:
        app: Azure-02
    spec:
      containers:
      - name: Azure-02
        image: Azure-02:latest
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 5
